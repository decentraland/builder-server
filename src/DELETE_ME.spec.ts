/**
 * This test exists so that at least 1 test exists inside the repo
 * causing CI not to break
 * Delete this file as soon as more tests are added
 */

it('DELETE ME', () => {
  expect(1).toBe(1)
})

export {}
